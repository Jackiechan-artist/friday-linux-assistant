# ──────────────────────────────────────────────────────────────
# FRIDAY AI ASSISTANT — Configuration
# Copy this file: cp .env.example .env
# Then fill in your actual keys below.
# ──────────────────────────────────────────────────────────────

# Your name (used in the AI's system prompt)
BOSS_NAME=YourName
BOSS_FULL_NAME=Your Full Name
ASSISTANT_NAME=FRIDAY
BOSS_LANGUAGE=hinglish
# Personality style: formal_sir | casual | friendly
BOSS_STYLE=formal_sir

# ── Brain 1 — Router / Chat ────────────────────────────────────
# Get a free key at: https://build.nvidia.com  (NVIDIA NIM)
# Alternatively use OpenRouter: https://openrouter.ai
NVIDIA_KEY_BRAIN1=your_nvidia_api_key_here
BRAIN1_BASE_URL=https://integrate.api.nvidia.com/v1
BRAIN1_MODEL=meta/llama-3.3-70b-instruct
BRAIN1_MODEL_FALLBACK=openai/gpt-oss-120b

# ── Brain 2 — LADA Executor / Planner ─────────────────────────
# Can reuse the same key as Brain 1 or use a separate one
NVIDIA_KEY_BRAIN2=your_nvidia_api_key_here
BRAIN2_BASE_URL=https://integrate.api.nvidia.com/v1
BRAIN2_MODEL=meta/llama-3.3-70b-instruct

# ── Brain 3 — Response Polisher ───────────────────────────────
NVIDIA_KEY_BRAIN3=your_nvidia_api_key_here
BRAIN3_BASE_URL=https://integrate.api.nvidia.com/v1
BRAIN3_MODEL=meta/llama-3.3-70b-instruct

# ── OpenRouter Fallback (if NVIDIA is down) ───────────────────
# Free tier available at: https://openrouter.ai
OPENROUTER_KEY=your_openrouter_key_here
OPENROUTER_MODEL=openai/gpt-4o-mini

# ── Groq Fallback (fast free tier) ────────────────────────────
# Get key at: https://console.groq.com
GROQ_KEY=your_groq_key_here
GROQ_MODEL=llama-3.1-70b-versatile

# ── STT Settings ───────────────────────────────────────────────
STT_LANGUAGE=en-IN
STT_TIMEOUT=8
STT_RETRY=3
STT_ENERGY_THRESHOLD=200
# Whisper model: tiny.en | base.en | small.en
# Smaller = faster, larger = more accurate
STT_WHISPER_MODEL=tiny.en

# ── TTS Settings ───────────────────────────────────────────────
TTS_SPEED=1.0
# en | hi | auto  (auto detects Hindi vs English per sentence)
TTS_LANGUAGE=auto

# ── Wake Word ──────────────────────────────────────────────────
# Get a FREE Picovoice key at: https://console.picovoice.ai
# The free tier covers personal/non-commercial use.
PICOVOICE_KEY=your_picovoice_key_here

# ── Screen Reading ─────────────────────────────────────────────
SCREEN_REFRESH=2
SCREEN_MAX_CHARS=800

# ── Feature Toggles ────────────────────────────────────────────
WEB_SEARCH=true
PLAN_CACHE=true
PLAN_CACHE_MAX_AGE_HOURS=24
PROACTIVE_MODE=true
PROACTIVE_INTERVAL=30

# ── Stop Keywords ──────────────────────────────────────────────
STOP_KEYWORDS=stop,ruko,ruk,bas,cancel,cancel karo,mat karo,nahi,hold on,wait

# ── Custom Context (optional) ─────────────────────────────────
# Extra context injected into the AI system prompt every time.
# Example: CUSTOM_CONTEXT=I am a Python developer. My default editor is VS Code.
CUSTOM_CONTEXT=
